providers = ["python"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd backend && pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python main.py"
